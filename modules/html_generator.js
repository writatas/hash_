const HTML_Generate_Objects = (...arr) => (function(...generate_array){
    arr = generate_array
    //render
    const  [player,enemy_que,clock] = [...arr]
    //create all of the necessary html elements if they don't already exist.
    console.log(player)
    

    


})(...arr)



export {HTML_Generate_Objects}